### Slots
| Name | Description |
|------|-------------|
<% slots.forEach(function(slot) { %>| `<%= slot.name %>`{.primary .text-primary} | <% if (!slot.description) { %>—<% } else { %><%= slot.description %><% } %> |
<% }); %>
