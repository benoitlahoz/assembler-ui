<%
function kebabCase(str) {
  return str
    .replace(/([a-z])([A-Z])/g, '$1-$2')
    .replace(/[\s_]+/g, '-')
    .toLowerCase();
}
%>
<% /* example-section.ejs : Affiche un exemple à partir de son nom */ %>
<% if (typeof exampleName !== 'undefined' && exampleName) { %>
::tabs
  :::tabs-item{icon="i-lucide-eye" label="Preview"}
    <<%= kebabCase(exampleName) %> />
  :::

  :::tabs-item{icon="i-lucide-code" label="Code"}
  ```vue
  <<%= exampleName %> />
  ```
  :::
::
<% } %>