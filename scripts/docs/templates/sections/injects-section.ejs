### Inject
| Key | Default | Type | Description |
|-----|--------|------|-------------|
<% injects.forEach(function(item) { %>| `<%= item.key %>`{.primary .text-primary} | <% if (typeof item.default === 'boolean' || typeof item.default === 'number') { %>`<%- item.default %>`<% } else if (item.default === '' || typeof item.default === 'undefined') { %>—<% } else { %>`<%- typeof escapePipe === 'function' ? escapePipe(item.default) : item.default %>`<% } %> | <% if (item.type === 'string' || item.type === 'number' || item.type === 'boolean') { %>`<%= item.type %>`<% } else if (!item.type || item.type === '') { %>—<% } else { %>`<%- typeof escapePipe === 'function' ? escapePipe(item.type) : item.type %>`<% } %> | <% if (!item.description) { %>—<% } else { %><%- item.description %><% } %> |
<% }); %>