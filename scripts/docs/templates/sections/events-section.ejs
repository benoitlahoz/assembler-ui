### Events
| Name | Description |
|------|-------------|
<% events.forEach(function(event) { %>| `<%= event.name %>`{.primary .text-primary} | <% if (!event.description) { %>—<% } else { %><%= event.description %><% } %> |
<% }); %>
