### Provide
| Key | Value | Type | Description |
|-----|-------|------|-------------|
<% provides.forEach(function(item) { %>| `<%= item.key %>`{.primary .text-primary} | <% if (typeof item.value === 'boolean' || typeof item.value === 'number') { %>`<%= item.value %>`<% } else if (item.value === '' || typeof item.value === 'undefined') { %><span style="color:#aaa">-</span><% } else { %>`<%= item.value %>`<% } %> | <% if (item.type === 'string' || item.type === 'number' || item.type === 'boolean') { %>`<%= item.type %>`<% } else if (!item.type || item.type === '') { %>-<% } else { %>`<%= item.type %>`<% } %> | <%= item.description %> |<% }); %>